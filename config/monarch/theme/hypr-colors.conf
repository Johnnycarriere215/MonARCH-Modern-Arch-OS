# GENERATED BY MONARCH — DO NOT EDIT
#
# Regenerate with: monarch theme apply <name>
#
# Every colour in MonARCH's Hyprland configuration resolves through this file.
# Nothing downstream hardcodes a hex value, so swapping themes is a matter of
# rewriting this one file and reloading.
#
# Until T3 ships the theme engine this is the checked-in default palette
# ("midnight"). It is seed-only: monarch-config-apply installs it when missing
# and never overwrites it, so an applied theme survives an update.

# ------------------------------------------------------------------ surfaces
$bg          = rgb(0d0f14)
$bg_alt      = rgb(151922)
$surface     = rgb(1c2130)
$overlay     = rgb(262d3f)

# --------------------------------------------------------------------- text
$fg          = rgb(d6dae4)
$fg_dim      = rgb(8b93a7)
$fg_faint    = rgb(5b6377)

# ------------------------------------------------------------------- accents
$accent      = rgb(7c6df2)
$accent_alt  = rgb(c9a227)

# --------------------------------------------------------------------- state
$red         = rgb(e05252)
$green       = rgb(4fb477)
$yellow      = rgb(d9a441)
$blue        = rgb(4f8ff7)
$magenta     = rgb(a978e8)
$cyan        = rgb(45b8bd)

# ------------------------------------------- alpha variants for borders/blur
# Hyprland gradients need rgba(), and the shadow needs a low-alpha black.
$accent_a    = rgba(7c6df2ee)
$accent_alt_a = rgba(c9a227ee)
$inactive_a  = rgba(262d3faa)
$shadow_a    = rgba(00000055)
